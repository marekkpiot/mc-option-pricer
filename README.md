# Monte Carlo Option Pricer

This project implements the pricing of European call and put options
using Monte Carlo simulation in Python.

## Current features

- European call payoff
- European put payoff

## Project structure

- `src/payoffs.py`: payoff functions
- `main.py`: basic tests
- `figures/`: generated figures

## Installation

```bash
python -m pip install -r requirements.txt